# InquiryFlow static site

Release: v4-web-20260912-1. Written workflow discovery only.

Publish this directory's website files at the existing GitHub Pages root. Preserve the configured custom domain. Do not upload internal project documents, credentials, operation ledgers, client material or test traces.

This website has no runtime processing engine, model endpoint, uploads, payments, automated sending or analytics. The synthetic example is a static excerpt, not a customer case. Contact links open the visitor's own mail application and ask for no customer records.

Before updating main, retain the current commit as a rollback reference, confirm no concurrent changes and run local checks. After publishing, verify the deployed release manifest and file hashes over validated HTTPS. A successful commit or build is not proof of domain or mailbox delivery.

Retire all four legacy files in downloads/: InquiryFlow_3_inquiry_template.csv, InquiryFlow_3_inquiry_template.xlsx, PASTE_3_SUMMARIES.txt and fictional_sample_analysis.csv. Deleting working-tree copies does not remove git history, caches or copies already downloaded.
